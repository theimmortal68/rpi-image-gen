---
name: fake-hwclock
mmdebstrap:
  variant: apt
  packages:
    - fake-hwclock
  customize-hooks:
    - date -d "@${SOURCE_DATE_EPOCH}" > $1/etc/fake-hwclock.data
    - $KS_HELPERS/enable-units "$1" fake-hwclock
