---
name: crowsnest
mmdebstrap:
  variant: apt
  packages:
    - git
    - make
    - crudini
    - bsdutils
    - findutils
    - v4l-utils
    - curl
    - build-essential
    - libevent-dev
    - libjpeg-dev
    - libbsd-dev
    - pkg-config
    - cmake
    - libavformat-dev
    - libavutil-dev
    - libavcodec-dev
    - libcamera-dev
    - libcamera-apps-lite
    - liblivemedia-dev
    - xxd
    - libssl-dev
  customize-hooks:
    # --- In-chroot: clone source (no fallback, no idempotence)
    - |
      echo "[crowsnest] clone repo"
      chroot "$1" env \
        IGUSER="${IGconf_device_user1}" \
        REPO="https://github.com/mainsail-crew/crowsnest.git" \
        sh -lc '
          set -eu
          runuser -u "$IGUSER" -- git clone --depth=1 "$REPO" "/home/$IGUSER/crowsnest"'

    # --- In-chroot: build & install as root (avoid systemctl in Makefile)
    - |
      echo "[crowsnest] make install"
      chroot "$1" sh -lc '
        set -eu
        cd "/home/'"${IGconf_device_user1}"'/crowsnest"
        # Build (if the project has a build step; harmless if not)
        make PREFIX=/usr SYSCONFDIR=/etc VERBOSE=1
        # Install into the image; prevent Makefile from calling systemctl
        NO_SYSTEMCTL=1 CROWSNEST_UNATTENDED=1 CROWSNEST_ADD_CROWSNEST_MOONRAKER=1 \
          make install PREFIX=/usr SYSCONFDIR=/etc VERBOSE=1'

    # --- Host-side: enable unit(s) explicitly
    - |
      echo "[crowsnest] enable service"
      $BDEBSTRAP_HOOKS/enable-units "$1" crowsnest
