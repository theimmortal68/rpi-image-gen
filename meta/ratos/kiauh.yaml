---
name: kiauh
mmdebstrap:
  variant: apt
  packages:
    - git
    - ca-certificates
  customize-hooks:
    - runuser -u "${IGconf_device_user1}" -- git clone --depth=1 https://github.com/dw-0/kiauh "/home/${IGconf_device_user1}/kiauh"
