# Defaults for the openssh-server meta layer.
#
# Provide a public key either as:
#  - a path to a .pub file, or
#  - the key string itself (ssh-ed25519/ssh-rsa ...)
#
# The loader exposes this as: KSconf_meta_ssh_pubkey
ssh_pubkey=
